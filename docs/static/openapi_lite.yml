swagger: '2.0'
info:
  title: HTTP API Console
  name: ''
  description: ''
paths:
  /hypergrid-ssn/hypergridssn/fee_settlement_bill:
    get:
      operationId: HypergridssnHypergridssnQuery_FeeSettlementBillAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              FeeSettlementBill:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    fromId:
                      type: string
                      format: uint64
                      title: from id in GridBlockFee
                    endId:
                      type: string
                      format: uint64
                      title: end id in GridBlockFee
                    bill:
                      type: string
                      title: >-
                        json [{"grid": "grid111","profit":100}, {"grid":
                        "grid222","profit":200}]
                    status:
                      type: integer
                      format: int32
                    creator:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/fee_settlement_bill/{id}:
    get:
      summary: Queries a list of FeeSettlementBill items.
      operationId: HypergridssnHypergridssnQuery_FeeSettlementBill
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              FeeSettlementBill:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  fromId:
                    type: string
                    format: uint64
                    title: from id in GridBlockFee
                  endId:
                    type: string
                    format: uint64
                    title: end id in GridBlockFee
                  bill:
                    type: string
                    title: >-
                      json [{"grid": "grid111","profit":100}, {"grid":
                      "grid222","profit":200}]
                  status:
                    type: integer
                    format: int32
                  creator:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/grid_block_fee:
    get:
      operationId: HypergridssnHypergridssnQuery_GridBlockFeeAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              GridBlockFee:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    grid:
                      type: string
                    slot:
                      type: string
                    blockhash:
                      type: string
                    blocktime:
                      type: integer
                      format: int32
                    fee:
                      type: string
                    creator:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/grid_block_fee/{id}:
    get:
      summary: Queries a list of GridBlockFee items.
      operationId: HypergridssnHypergridssnQuery_GridBlockFee
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              GridBlockFee:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  grid:
                    type: string
                  slot:
                    type: string
                  blockhash:
                    type: string
                  blocktime:
                    type: integer
                    format: int32
                  fee:
                    type: string
                  creator:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/grid_inbox:
    get:
      operationId: HypergridssnHypergridssnQuery_GridInboxAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              GridInbox:
                type: array
                items:
                  type: object
                  properties:
                    id:
                      type: string
                      format: uint64
                    grid:
                      type: string
                    account:
                      type: string
                    slot:
                      type: string
                    hash:
                      type: string
                    creator:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/grid_inbox/{id}:
    get:
      summary: Queries a list of GridInbox items.
      operationId: HypergridssnHypergridssnQuery_GridInbox
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              GridInbox:
                type: object
                properties:
                  id:
                    type: string
                    format: uint64
                  grid:
                    type: string
                  account:
                    type: string
                  slot:
                    type: string
                  hash:
                    type: string
                  creator:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: id
          in: path
          required: true
          type: string
          format: uint64
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/hypergrid_node:
    get:
      operationId: HypergridssnHypergridssnQuery_HypergridNodeAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              hypergridNode:
                type: array
                items:
                  type: object
                  properties:
                    pubkey:
                      type: string
                      title: Grid's pubkey
                    name:
                      type: string
                    rpc:
                      type: string
                      title: Grid RPC
                    role:
                      type: integer
                      format: int32
                      title: >-
                        0: unknown, 1: HSSN, 2: Sonic Grid, 3: Grid, 4: Solana
                        L1
                    data_account:
                      type: string
                      title: data account for storing bill
                    starttime:
                      type: integer
                      format: int32
                    creator:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/hypergrid_node/{pubkey}:
    get:
      summary: Queries a list of HypergridNode items.
      operationId: HypergridssnHypergridssnQuery_HypergridNode
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              hypergridNode:
                type: object
                properties:
                  pubkey:
                    type: string
                    title: Grid's pubkey
                  name:
                    type: string
                  rpc:
                    type: string
                    title: Grid RPC
                  role:
                    type: integer
                    format: int32
                    title: '0: unknown, 1: HSSN, 2: Sonic Grid, 3: Grid, 4: Solana L1'
                  data_account:
                    type: string
                    title: data account for storing bill
                  starttime:
                    type: integer
                    format: int32
                  creator:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pubkey
          in: path
          required: true
          type: string
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/solana_account:
    get:
      operationId: HypergridssnHypergridssnQuery_SolanaAccountAll
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              solanaAccount:
                type: array
                items:
                  type: object
                  properties:
                    address:
                      type: string
                      title: Solana address
                    version:
                      type: string
                    source:
                      type: string
                      title: '-> HypergridNode'
                    slot:
                      type: string
                    value:
                      type: string
                      title: Solana Account Info
                    creator:
                      type: string
              pagination:
                type: object
                properties:
                  next_key:
                    type: string
                    format: byte
                    description: |-
                      next_key is the key to be passed to PageRequest.key to
                      query the next page most efficiently. It will be empty if
                      there are no more results.
                  total:
                    type: string
                    format: uint64
                    title: >-
                      total is total number of results available if
                      PageRequest.count_total

                      was set, its value is undefined otherwise
                description: >-
                  PageResponse is to be embedded in gRPC response messages where
                  the

                  corresponding request message has used PageRequest.

                   message SomeResponse {
                           repeated Bar results = 1;
                           PageResponse page = 2;
                   }
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: pagination.key
          description: |-
            key is a value returned in PageResponse.next_key to begin
            querying the next page most efficiently. Only one of offset or key
            should be set.
          in: query
          required: false
          type: string
          format: byte
        - name: pagination.offset
          description: >-
            offset is a numeric offset that can be used when key is unavailable.

            It is less efficient than using key. Only one of offset or key
            should

            be set.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.limit
          description: >-
            limit is the total number of results to be returned in the result
            page.

            If left empty it will default to a value to be set by each app.
          in: query
          required: false
          type: string
          format: uint64
        - name: pagination.count_total
          description: >-
            count_total is set to true  to indicate that the result set should
            include

            a count of the total number of items available for pagination in
            UIs.

            count_total is only respected when offset is used. It is ignored
            when key

            is set.
          in: query
          required: false
          type: boolean
        - name: pagination.reverse
          description: >-
            reverse is set to true if results are to be returned in the
            descending order.


            Since: cosmos-sdk 0.43
          in: query
          required: false
          type: boolean
      tags:
        - Query
  /hypergrid-ssn/hypergridssn/solana_account/{address}/{version}:
    get:
      summary: Queries a list of SolanaAccount items.
      operationId: HypergridssnHypergridssnQuery_SolanaAccount
      responses:
        '200':
          description: A successful response.
          schema:
            type: object
            properties:
              solanaAccount:
                type: object
                properties:
                  address:
                    type: string
                    title: Solana address
                  version:
                    type: string
                  source:
                    type: string
                    title: '-> HypergridNode'
                  slot:
                    type: string
                  value:
                    type: string
                    title: Solana Account Info
                  creator:
                    type: string
        default:
          description: An unexpected error response.
          schema:
            type: object
            properties:
              code:
                type: integer
                format: int32
              message:
                type: string
              details:
                type: array
                items:
                  type: object
                  properties:
                    '@type':
                      type: string
                  additionalProperties: {}
      parameters:
        - name: address
          in: path
          required: true
          type: string
        - name: version
          in: path
          required: true
          type: string
      tags:
        - Query
definitions:
  hypergridssn.hypergridssn.FeeSettlementBill:
    type: object
    properties:
      id:
        type: string
        format: uint64
      fromId:
        type: string
        format: uint64
        title: from id in GridBlockFee
      endId:
        type: string
        format: uint64
        title: end id in GridBlockFee
      bill:
        type: string
        title: >-
          json [{"grid": "grid111","profit":100}, {"grid":
          "grid222","profit":200}]
      status:
        type: integer
        format: int32
      creator:
        type: string
  hypergridssn.hypergridssn.GridBlockFee:
    type: object
    properties:
      id:
        type: string
        format: uint64
      grid:
        type: string
      slot:
        type: string
      blockhash:
        type: string
      blocktime:
        type: integer
        format: int32
      fee:
        type: string
      creator:
        type: string
  hypergridssn.hypergridssn.GridInbox:
    type: object
    properties:
      id:
        type: string
        format: uint64
      grid:
        type: string
      account:
        type: string
      slot:
        type: string
      hash:
        type: string
      creator:
        type: string
  hypergridssn.hypergridssn.HypergridNode:
    type: object
    properties:
      pubkey:
        type: string
        title: Grid's pubkey
      name:
        type: string
      rpc:
        type: string
        title: Grid RPC
      role:
        type: integer
        format: int32
        title: '0: unknown, 1: HSSN, 2: Sonic Grid, 3: Grid, 4: Solana L1'
      data_account:
        type: string
        title: data account for storing bill
      starttime:
        type: integer
        format: int32
      creator:
        type: string
  hypergridssn.hypergridssn.QueryAllFeeSettlementBillResponse:
    type: object
    properties:
      FeeSettlementBill:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            fromId:
              type: string
              format: uint64
              title: from id in GridBlockFee
            endId:
              type: string
              format: uint64
              title: end id in GridBlockFee
            bill:
              type: string
              title: >-
                json [{"grid": "grid111","profit":100}, {"grid":
                "grid222","profit":200}]
            status:
              type: integer
              format: int32
            creator:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  hypergridssn.hypergridssn.QueryAllGridBlockFeeResponse:
    type: object
    properties:
      GridBlockFee:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            grid:
              type: string
            slot:
              type: string
            blockhash:
              type: string
            blocktime:
              type: integer
              format: int32
            fee:
              type: string
            creator:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  hypergridssn.hypergridssn.QueryAllGridInboxResponse:
    type: object
    properties:
      GridInbox:
        type: array
        items:
          type: object
          properties:
            id:
              type: string
              format: uint64
            grid:
              type: string
            account:
              type: string
            slot:
              type: string
            hash:
              type: string
            creator:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  hypergridssn.hypergridssn.QueryAllHypergridNodeResponse:
    type: object
    properties:
      hypergridNode:
        type: array
        items:
          type: object
          properties:
            pubkey:
              type: string
              title: Grid's pubkey
            name:
              type: string
            rpc:
              type: string
              title: Grid RPC
            role:
              type: integer
              format: int32
              title: '0: unknown, 1: HSSN, 2: Sonic Grid, 3: Grid, 4: Solana L1'
            data_account:
              type: string
              title: data account for storing bill
            starttime:
              type: integer
              format: int32
            creator:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  hypergridssn.hypergridssn.QueryAllSolanaAccountResponse:
    type: object
    properties:
      solanaAccount:
        type: array
        items:
          type: object
          properties:
            address:
              type: string
              title: Solana address
            version:
              type: string
            source:
              type: string
              title: '-> HypergridNode'
            slot:
              type: string
            value:
              type: string
              title: Solana Account Info
            creator:
              type: string
      pagination:
        type: object
        properties:
          next_key:
            type: string
            format: byte
            description: |-
              next_key is the key to be passed to PageRequest.key to
              query the next page most efficiently. It will be empty if
              there are no more results.
          total:
            type: string
            format: uint64
            title: >-
              total is total number of results available if
              PageRequest.count_total

              was set, its value is undefined otherwise
        description: |-
          PageResponse is to be embedded in gRPC response messages where the
          corresponding request message has used PageRequest.

           message SomeResponse {
                   repeated Bar results = 1;
                   PageResponse page = 2;
           }
  hypergridssn.hypergridssn.QueryGetFeeSettlementBillResponse:
    type: object
    properties:
      FeeSettlementBill:
        type: object
        properties:
          id:
            type: string
            format: uint64
          fromId:
            type: string
            format: uint64
            title: from id in GridBlockFee
          endId:
            type: string
            format: uint64
            title: end id in GridBlockFee
          bill:
            type: string
            title: >-
              json [{"grid": "grid111","profit":100}, {"grid":
              "grid222","profit":200}]
          status:
            type: integer
            format: int32
          creator:
            type: string
  hypergridssn.hypergridssn.QueryGetGridBlockFeeResponse:
    type: object
    properties:
      GridBlockFee:
        type: object
        properties:
          id:
            type: string
            format: uint64
          grid:
            type: string
          slot:
            type: string
          blockhash:
            type: string
          blocktime:
            type: integer
            format: int32
          fee:
            type: string
          creator:
            type: string
  hypergridssn.hypergridssn.QueryGetGridInboxResponse:
    type: object
    properties:
      GridInbox:
        type: object
        properties:
          id:
            type: string
            format: uint64
          grid:
            type: string
          account:
            type: string
          slot:
            type: string
          hash:
            type: string
          creator:
            type: string
  hypergridssn.hypergridssn.QueryGetHypergridNodeResponse:
    type: object
    properties:
      hypergridNode:
        type: object
        properties:
          pubkey:
            type: string
            title: Grid's pubkey
          name:
            type: string
          rpc:
            type: string
            title: Grid RPC
          role:
            type: integer
            format: int32
            title: '0: unknown, 1: HSSN, 2: Sonic Grid, 3: Grid, 4: Solana L1'
          data_account:
            type: string
            title: data account for storing bill
          starttime:
            type: integer
            format: int32
          creator:
            type: string
  hypergridssn.hypergridssn.QueryGetSolanaAccountResponse:
    type: object
    properties:
      solanaAccount:
        type: object
        properties:
          address:
            type: string
            title: Solana address
          version:
            type: string
          source:
            type: string
            title: '-> HypergridNode'
          slot:
            type: string
          value:
            type: string
            title: Solana Account Info
          creator:
            type: string
  hypergridssn.hypergridssn.SolanaAccount:
    type: object
    properties:
      address:
        type: string
        title: Solana address
      version:
        type: string
      source:
        type: string
        title: '-> HypergridNode'
      slot:
        type: string
      value:
        type: string
        title: Solana Account Info
      creator:
        type: string
